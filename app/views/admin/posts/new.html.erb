<section>
<%= form_for [:admin, @post], :url => admin_posts_url(@post) do |f| %>
    <p>
        <%= f.text_field :name %>
    </p>
    <p>
        <%= f.text_area :body %>
    </p>

    <p>
        <%= f.submit %>
    </p>
<% end %>
</section>